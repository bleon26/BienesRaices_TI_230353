header.bg-white.p-4 
    .container.mx-auto.flex.justify-between.items-center 
        a(href="/")
            h1.text-4xl.text-cyan.font-extrabold.text-center Bienes 
                span.font-normal Raíces

        nav(class="my-5 text-sm md:flex md:items-center md:gap-3 font-bold text-golden hidden") 
            a(href="/mis-propiedades") Mis propiedades 
            a(href="") Mi perfil
            form(action="/auth/cerrar-sesion" method="POST")  
                input(type="hidden" name="_csrf" value= csrfToken)
                input(type="submit" class="bg-golden py-2 px-10 rounded-lg text-white hover:bg-orange_red " value="Cerrar Sesión")    
